# Segredos e config só-local (sobrescreve runtime-config no navegador)
.env
js/config.js

# Ferramentas n8n e scripts de build — só na máquina local (não publicar no Git)
ferramentas-n8n/
scripts/

# Editor
.vscode/
